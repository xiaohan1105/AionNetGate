<Window x:Class="AionGate.Admin.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ui="http://schemas.modernwpf.com/2019"
        xmlns:lvc="clr-namespace:LiveChartsCore.SkiaSharpView.WPF;assembly=LiveChartsCore.SkiaSharpView.WPF"
        ui:WindowHelper.UseModernWindowStyle="True"
        Title="AionGate 管理控制台" Height="900" Width="1600"
        WindowStartupLocation="CenterScreen"
        Background="{DynamicResource SystemControlPageBackgroundChromeLowBrush}">

    <Window.Resources>
        <!-- 卡片样式 -->
        <Style x:Key="CardStyle" TargetType="Border">
            <Setter Property="Background" Value="{DynamicResource SystemControlBackgroundAltHighBrush}"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="Padding" Value="16"/>
            <Setter Property="Margin" Value="8"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="Black" Opacity="0.1" BlurRadius="10" ShadowDepth="2"/>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- 统计卡片样式 -->
        <Style x:Key="StatCardStyle" TargetType="Border" BasedOn="{StaticResource CardStyle}">
            <Setter Property="Padding" Value="20"/>
            <Setter Property="MinHeight" Value="120"/>
        </Style>

        <!-- 按钮组样式 -->
        <Style x:Key="ActionButtonStyle" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="Margin" Value="4"/>
            <Setter Property="Padding" Value="16,8"/>
            <Setter Property="MinWidth" Value="120"/>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- 顶部工具栏 -->
        <Border Grid.Row="0" Background="{DynamicResource SystemControlBackgroundAccentBrush}" Padding="16,12">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Orientation="Horizontal">
                    <TextBlock Text="⚡" FontSize="24" VerticalAlignment="Center" Margin="0,0,8,0"/>
                    <TextBlock Text="AionGate 管理控制台" FontSize="20" FontWeight="Bold"
                               Foreground="White" VerticalAlignment="Center"/>
                </StackPanel>

                <StackPanel Grid.Column="2" Orientation="Horizontal" Spacing="12">
                    <Button Content="🔄 刷新" Style="{StaticResource AccentButtonStyle}" Click="RefreshAll_Click"/>
                    <Button Content="📢 全服公告" Style="{StaticResource AccentButtonStyle}" Click="SendAnnouncement_Click"/>
                    <Button Content="⚙️ 设置" Click="OpenSettings_Click"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- 主内容区 -->
        <ui:NavigationView Grid.Row="1"
                          x:Name="NavView"
                          PaneDisplayMode="Left"
                          IsBackButtonVisible="Collapsed"
                          IsSettingsVisible="False"
                          SelectionChanged="NavView_SelectionChanged">

            <ui:NavigationView.MenuItems>
                <ui:NavigationViewItem Icon="Home" Content="仪表板" Tag="Dashboard"/>
                <ui:NavigationViewItem Icon="People" Content="玩家管理" Tag="Players"/>
                <ui:NavigationViewItem Icon="Shop" Content="商城管理" Tag="Shop"/>
                <ui:NavigationViewItem Icon="Message" Content="公告管理" Tag="Announcements"/>
                <ui:NavigationViewItem Icon="Link" Content="渠道分发" Tag="Channels"/>
                <ui:NavigationViewItem Icon="Download" Content="热更新" Tag="Updates"/>
                <ui:NavigationViewItem Icon="Calculator" Content="数据统计" Tag="Statistics"/>
                <ui:NavigationViewItem Icon="Warning" Content="外挂检测" Tag="AntiCheat"/>
                <ui:NavigationViewItem Icon="Admin" Content="系统工具" Tag="Tools"/>
            </ui:NavigationView.MenuItems>

            <Frame x:Name="ContentFrame" NavigationUIVisibility="Hidden"/>
        </ui:NavigationView>
    </Grid>
</Window>
